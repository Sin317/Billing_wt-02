A simple bill analysis project.

Input your bill details and the model predicts your score between 0,1 and 2 which would be good, bad and worse respectively.

Commands to run:

-Frontend:
npm install

npm run build

-Backend:
pipenv install

pipenv run python manage.py runserver